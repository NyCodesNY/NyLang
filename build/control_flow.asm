; ─── Generated by NyLang Compiler v0.1 ───

extern printf

section .data
_str_0: db 'T', 'e', 's', 't', 'i', 'n', 'g', ' ', 'i', 'f', ' ', 's', 't', 'a', 't', 'e', 'm', 'e', 'n', 't', '.', '.', '.', 10, 0
_str_1: db 'S', 'U', 'C', 'C', 'E', 'S', 'S', ':', ' ', '1', ' ', 'i', 's', ' ', 'l', 'e', 's', 's', ' ', 't', 'h', 'a', 'n', ' ', '2', 10, 0
_str_2: db 'F', 'A', 'I', 'L', ':', ' ', '1', ' ', 'i', 's', ' ', 'n', 'o', 't', ' ', 'l', 'e', 's', 's', ' ', 't', 'h', 'a', 'n', ' ', '2', 10, 0
_str_3: db 'T', 'e', 's', 't', 'i', 'n', 'g', ' ', 'w', 'h', 'i', 'l', 'e', ' ', 'l', 'o', 'o', 'p', '.', '.', '.', 10, 0
_str_4: db 'S', 'U', 'C', 'C', 'E', 'S', 'S', ':', ' ', '5', ' ', 'e', 'q', 'u', 'a', 'l', 's', ' ', '5', 10, 0

section .text
global main
main:
    push rbp
    mov rbp, rsp
    ; System.Print("Testing if statement...")
    lea rdi, [rel _str_0]
    xor eax, eax
    call printf wrt ..plt
    mov rax, 1
    push rax
    mov rax, 2
    mov rcx, rax
    pop rax
    cmp rax, rcx
    setl al
    movzx rax, al
    cmp rax, 0
    je .L_else_0
    ; System.Print("SUCCESS: 1 is less than 2")
    lea rdi, [rel _str_1]
    xor eax, eax
    call printf wrt ..plt
    jmp .L_end_0
.L_else_0:
    ; System.Print("FAIL: 1 is not less than 2")
    lea rdi, [rel _str_2]
    xor eax, eax
    call printf wrt ..plt
.L_end_0:
    ; System.Print("Testing while loop...")
    lea rdi, [rel _str_3]
    xor eax, eax
    call printf wrt ..plt
    mov rax, 5
    push rax
    mov rax, 5
    mov rcx, rax
    pop rax
    cmp rax, rcx
    sete al
    movzx rax, al
    cmp rax, 0
    je .L_end_1
    ; System.Print("SUCCESS: 5 equals 5")
    lea rdi, [rel _str_4]
    xor eax, eax
    call printf wrt ..plt
.L_end_1:
    xor eax, eax
    mov rsp, rbp
    pop rbp
    ret

