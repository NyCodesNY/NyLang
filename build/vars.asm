; ─── Generated by NyLang Compiler v0.1 ───

extern printf

section .data
.fmt_int: db "%d", 10, 0
.fmt_str: db "%s", 10, 0
_str_0: db 'T', 'e', 's', 't', 'i', 'n', 'g', ' ', 'i', 'n', 't', 'e', 'g', 'e', 'r', ' ', 'p', 'r', 'i', 'n', 't', 'i', 'n', 'g', ':', 0
_str_1: db 'H', 'e', 'l', 'l', 'o', ' ', 'f', 'r', 'o', 'm', ' ', 'N', 'y', 'L', 'a', 'n', 'g', ' ', 'v', 'a', 'r', 'i', 'a', 'b', 'l', 'e', 's', '!', 0
_str_2: db 'T', 'e', 's', 't', 'i', 'n', 'g', ' ', 's', 't', 'r', 'i', 'n', 'g', ' ', 'p', 'r', 'i', 'n', 't', 'i', 'n', 'g', ':', 0

section .text
global main
main:
    push rbp
    mov rbp, rsp
    mov rax, 42
    sub rsp, 8
    mov [rbp - 8], rax
    lea rax, [rel _str_0]
    mov rsi, rax
    lea rdi, [rel .fmt_str]
    xor eax, eax
    call printf wrt ..plt
    mov rax, [rbp - 8]
    mov rsi, rax
    lea rdi, [rel .fmt_int]
    xor eax, eax
    call printf wrt ..plt
    lea rax, [rel _str_1]
    sub rsp, 8
    mov [rbp - 16], rax
    lea rax, [rel _str_2]
    mov rsi, rax
    lea rdi, [rel .fmt_str]
    xor eax, eax
    call printf wrt ..plt
    mov rax, [rbp - 16]
    mov rsi, rax
    lea rdi, [rel .fmt_str]
    xor eax, eax
    call printf wrt ..plt
    xor eax, eax
    mov rsp, rbp
    pop rbp
    ret

